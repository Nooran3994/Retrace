"""Retrace — Terminal Flight Recorder.

Record. Replay. Analyze. Secure.

Offline-first terminal intelligence: captures shell history and live
sessions, redacts secrets at capture time, stores locally in SQLite,
and never makes network calls unless explicitly exported by the user.
"""

__version__ = "0.1.0"