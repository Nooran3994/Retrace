[project]
name = "retrace"
version = "0.1.0"
description = "Terminal Flight Recorder — Record. Replay. Analyze. Secure."
requires-python = ">=3.9"
license = { text = "MIT" }

[project.scripts]
retrace = "retrace.cli:main"
rec = "retrace.cli:main"

[tool.setuptools.packages.find]
include = ["retrace*"]